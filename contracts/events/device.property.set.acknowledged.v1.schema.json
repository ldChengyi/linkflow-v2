{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://linkflow.dev/contracts/events/lf.v1.device.property.set.acknowledged",
    "title": "device.property.set.acknowledged v1",
    "description": "Emitted when a device acknowledges a cloud-to-device property set command. The event records whether the command succeeded and can include the affected properties and the original protocol payload.",
    "type": "object",
    "required": [
        "tenant_id",
        "product_id",
        "device_id",
        "tenant_slug",
        "device_slug",
        "product_key",
        "protocol",
        "success"
    ],
    "additionalProperties": false,
    "properties": {
        "tenant_id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "description": "Tenant boundary identifier mirrored from the envelope."
        },
        "product_id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "description": "Stable internal product UUID resolved by EMQX auth."
        },
        "device_id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "description": "Stable internal device UUID resolved by EMQX auth."
        },
        "tenant_slug": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "Platform-facing tenant slug used at protocol boundaries. Producers should set this from the MQTT topic segment; consumers can map it to the envelope tenant_id when needed."
        },
        "device_slug": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "description": "Platform-facing device slug within the tenant scope. Combined with tenant_id, uniquely identifies a device at protocol boundaries."
        },
        "product_key": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "Identifier of the product (device model)."
        },
        "protocol": {
            "type": "string",
            "minLength": 2,
            "maxLength": 32,
            "pattern": "^[a-z][a-z0-9-]*$",
            "description": "Protocol used by the device acknowledgement. For this MQTT route the value is mqtt."
        },
        "success": {
            "type": "boolean",
            "description": "Whether the device accepted and applied the property set command."
        },
        "code": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "description": "Optional device or protocol result code."
        },
        "message": {
            "type": "string",
            "maxLength": 1024,
            "description": "Optional human-readable result message."
        },
        "properties": {
            "type": "object",
            "description": "Properties involved in the set acknowledgement, when reported by the device.",
            "patternProperties": {
                "^[a-z][a-z0-9_]*$": {
                    "oneOf": [
                        {
                            "type": "number"
                        },
                        {
                            "type": "boolean"
                        },
                        {
                            "type": "string",
                            "maxLength": 1024
                        }
                    ]
                }
            },
            "additionalProperties": false
        },
        "raw": {
            "type": "object",
            "description": "Original protocol payload fields captured by the gateway. Consumers should not depend on specific fields here.",
            "additionalProperties": true
        }
    }
}
